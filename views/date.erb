<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>Today</h1>
    <%date = Time.now %>
    <p>The date is <%= date.strftime("%A")%>, <%= date.strftime("%B") %> <%= date.strftime("%d") %>, <%= date.strftime("%Y") %></p>
  </body>
</html>
